<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <com.justes.yogabook.holo_keyboard.wrapper.AvailableLayouts
        android:key="@string/available_layouts_key"
        android:persistent="true"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/add_layout_label" />

    <com.justes.yogabook.holo_keyboard.wrapper.EnabledLayouts
        android:defaultValue="english"
        android:key="@string/enabled_layouts_key"
        android:persistent="true"
        android:title="@null" />

</PreferenceScreen>
